#pragma once

// No WiFi credentials needed — communication is over BLE.
// The BLE device name is hardcoded as "M5Paper-Claude" in ble_nus.c.
// The Mac-side daemon (m5paper_ble_daemon.py) scans for that name.
